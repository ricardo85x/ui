import { createGlobalStyle } from "styled-components";

// Global style for BeTheHero components
export const BeTheHeroPageStyles = createGlobalStyle`
 body {
    background: var(--gray-100);
  }
  body, input, textarea, button {
    font-family: 'Roboto';
  }
`;
